## SGF – Fresh Start

Purpose:
Internal web app for staff to view and edit projects.

Phase 1 goal:
A web app that displays and edits project data stored in a single shared database.

Access:
Users can access the app from anywhere via a web browser.

Architecture:
- Backend server (Node.js + Express)
- Shared cloud database (Postgres)
- Simple frontend UI

Principles:
- Simple first
- One shared database
- No legacy assumptions


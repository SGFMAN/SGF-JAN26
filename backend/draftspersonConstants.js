/**
 * Canonical value stored in `projects.draftsperson` when no person is assigned.
 * Keep in sync with `frontend/src/utils/draftspersonSentinel.js`.
 */
module.exports.DRAFTSPERSON_UNASSIGNED = "Select draftsperson...";

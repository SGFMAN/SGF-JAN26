/**
 * Windows 3.1 pointer — 12×21 px bitmap in public/cursors/retro-win31-arrow.png.
 * Regenerate: node scripts/generate-retro-cursor.mjs
 */
const TIP_X = 2;
const TIP_Y = 2;

/** CSS cursor — hotspot at the arrow tip (top-left). */
export const RETRO_WIN31_CURSOR = `url("/cursors/retro-win31-arrow.png") ${TIP_X} ${TIP_Y}, default`;

export const RETRO_THEME_ID = "retro";
